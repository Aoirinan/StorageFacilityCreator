{
  "name": "firestore-rules-test",
  "version": "1.0.0",
  "private": true,
  "description": "Firestore security rules unit tests",
  "scripts": {
    "test:inner": "node --test test/*.test.mjs",
    "test": "node scripts/run-with-firestore-emulator.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0"
  }
}
